# How to use it

-   to compile the project use:

    ```bash
    make compile
    ```

-   to run the compiled file:

    ```bash
    make run
    ```

-   to compile and run the project
    ```
    make
    ```
